server.port=8081
spring.application.name=PayPal

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/paypal
spring.datasource.username=root
spring.datasource.password=yash@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

#Logging
logging.level.org.hibernate.orm.connections.pooling=ERROR
logging.level.org.hibernate.orm.deprecation=ERROR

paypal.jwt.secret=paypalPaymentApplication!@1277002025SecureKey!!
paypal.jwt.expiration=86400000
paypal.jwt.cookieName=paypal-jwt
# set to false for local development over HTTP; set true in production (HTTPS)
paypal.jwt.secure=false